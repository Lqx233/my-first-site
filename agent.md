项目名称
自习室预约计时小程序（商家端 + 用户端 + 后台管理）

一、产品定位与目标
1.1 产品定位
面向自习室商家与用户的“选座预约 + 计时计费 + 卡券售卖 + 座位后台管理”一体化小程序。核心差异化在于：商家可自主创建/编辑自习室图纸（座位布局），并基于座位进行精细化预约与运营。

1.2 产品目标
商家目标：快速搭建自习室座位图、灵活上架座位与时长卡、实时管理座位状态与订单。
用户目标：直观选座预约、到场开始自动计时、可提前结束/延长、清晰查看消费与权益。
平台目标：形成“座位供给—预约使用—计时结算—复购卡券”的闭环，提升商家运营效率与用户复购率。
1.3 成功指标（建议）
转化：浏览→预约下单转化率、卡券购买转化率
履约：预约到场率、准时开始率、提前结束率
运营：上架座位数量、图纸编辑完成率、异常处理时长
复购：月卡/季卡/年卡续费率、次月留存
二、目标用户
2.1 用户端（自习者）
需求：快速找到合适门店/座位、可视化选座、可靠计时、费用透明、可延长与提前结束。
特征：对环境安静度/座位位置（靠窗、插座、角落）敏感；常有固定学习时段；愿意购买月/季卡。
2.2 商家端（门店经营者/店员）
需求：灵活创建与调整座位布局；对外上架发布；管理预约与在座状态；上架时长卡与会员卡。
特征：座位数量与布局会变化（维护、包场、临时封座）；希望减少人工登记与纠纷。
2.3 后台管理员（可选）
需求：门店/商品/订单/用户/座位异常统一治理；数据看板；风控与客服处理。
三、核心功能需求
3.1 商家：自习室图纸（布局）管理（核心）
创建图纸：设置房间尺寸（宽高/比例）、网格或自由画布、背景（可选）。
布局组件：座位（单人/双人/卡座）、通道、墙体/隔断、窗户、插座标识、前台/门等标识物。
座位属性：
座位编号、名称
位置坐标、朝向（可选）
可预约状态（可上架/仅内部/停用）
标签：靠窗/插座/安静区/可交流区等
价格策略绑定（按小时/按场次/按卡券抵扣规则）
草稿与发布：
保存草稿（不影响线上）
预览（模拟用户端选座）
上架发布（生效版本）
版本管理（回滚到上个发布版本）
编辑约束：
已有未来预约的座位：允许改名/标签；禁止删除或移动到冲突区域（需提示与处理方案：封座/改签/退款）。
3.2 用户：浏览与选座预约
门店/自习室入口：选择门店→进入座位图。
座位图浏览：查看座位分布与状态（空闲/可预约/已预约/使用中/封禁）。
预约创建：
选择座位、日期、开始时间、时长（或结束时间）
价格预估（原价/优惠/卡券抵扣/应付）
下单支付（微信支付）
预约规则（建议可配置）：
最早可预约时间、最晚可预约时间
取消/改签规则（提前多少分钟可取消、手续费）
超时未到场自动取消或转为占座（可配置）
3.3 用户：开始后自动计时（核心）
到场开始：
方式A：点击“到店开始”（基于商家允许的地理围栏/蓝牙/扫码校验，至少一种）
方式B：扫座位二维码开始（推荐，定位更准）
自动计时：
开始后显示计时器（已用时、预计结束、费用/权益扣减）
到达预约结束时间：自动结束并结算（或进入“待处理”）
提前结束：
用户发起提前结束→确认→结算
退款/计费策略可配置：按分钟计费、按半小时进位、最低消费等
延长时间（核心）：
条件：当前座位在延长时段内无他人预约/无商家封座
操作：选择延长时长→补差价支付或扣卡→更新结束时间
3.4 商家：座位状态管理（核心）
实时座位状态面板：空闲/已预约待到/使用中/即将结束/异常（超时未离场、强制占用等）。
商家操作：
封座/解封（维护、清洁、包场）
强制结束（异常处理，需记录原因）
手动开始/手动结束（兜底）
改签/换座（与用户确认后执行）
异常与告警：
预约到时未开始
使用到时未结束（离场确认/超时计费规则）
支付失败/扣卡失败
3.5 商品与卡券（商品列表页核心承载）
商品类型：
时长卡：X小时/次卡（可配置有效期、使用范围）
月卡/季卡/年卡：有效期权益（不限次/每日上限/时段限制）
商家定价与上架：
原价、售价、库存（可选）、限购
上下架、排序、推荐标签
用户购买与使用：
购买记录、有效期、剩余时长/次数
预约时自动推荐可用权益，支持手动选择
3.6 个人中心（用户）
我的预约：待开始/进行中/已完成/已取消
我的卡券：时长卡、月/季/年卡、到期提醒
订单与发票（可选）：订单列表、支付明细
常用信息：手机号、通知授权、隐私设置
四、信息架构与页面结构
4.1 用户端 IA
首页
门店选择/定位推荐
今日预约入口（有则置顶）
快捷入口：选座预约、购买卡券、我的预约
商品列表页
时长卡列表
月/季/年卡列表
商品详情（弹层或独立页）
下单支付
个人中心
我的预约（列表/详情）
进行中计时页（可从通知/首页进入）
我的卡券
订单记录
设置/客服
4.2 商家端（小程序内管理端或独立后台 H5）
门店管理
图纸/座位布局编辑器
草稿管理
发布/版本
座位状态看板（实时）
预约/订单管理
商品管理（时长卡、月/季/年卡）
营收与数据（可选）
五、页面说明（逐页说明）
说明格式统一：目标 / 核心模块 / 主要操作 / 关键规则与状态

5.1 首页（用户端）
目标：快速进入“选座预约”与“进行中计时”，提升下单效率。
核心模块：
顶部：定位/门店切换、公告（规则/活动）
中部：今日预约卡片（待开始/进行中/即将结束）
快捷入口：选座预约、购买卡券、我的预约
门店推荐/列表（距离、评分、可用座位数）
主要操作：
选择门店→进入座位图
点击“进行中”→计时页
点击“购买卡券”→商品列表页
关键规则与状态：
未授权定位：展示手动选择门店
有进行中订单：首页置顶并提供快捷“延长/提前结束”
5.2 商品列表页（用户端）
目标：承载商家可自主定价上架的时长卡与会员卡售卖，提高复购。
核心模块：
分类切换：时长卡 / 月卡 / 季卡 / 年卡（或“会员卡”）
商品卡片：名称、价格、优惠、权益简述、有效期
筛选/排序（可选）：推荐、销量、价格
主要操作：
查看商品详情→确认购买→支付
查看我的卡券→跳转个人中心
关键规则与状态：
商品售罄/下架：置灰不可购买
权益冲突提示：已有有效月卡时再次购买的叠加规则（顺延/并行/不可叠加）需明确
5.3 个人中心（用户端）
目标：集中管理预约、计时、卡券与订单。
核心模块：
用户信息：头像昵称、绑定手机号
我的预约：待开始/进行中/已完成
我的卡券：剩余时长/有效期
订单记录、客服与设置
主要操作：
进入预约详情：改签/取消（符合规则时）
进入进行中计时页：延长/提前结束
卡券查看与使用说明
关键规则与状态：
通知订阅：计时开始、即将结束、到期提醒（引导授权）
5.4 座位图页（用户端，选座预约）
目标：可视化选择座位并完成预约支付。
核心模块：
画布：座位布局、区域标签、缩放/拖拽
状态图例：空闲/可预约/已预约/使用中/封座
底部预约面板：日期、时间、时长、价格预估、权益选择
主要操作：
点击座位→查看座位信息（标签、价格、可用时段）
选择时间与时长→创建订单→支付
关键规则与状态：
冲突校验：提交前二次校验座位时段是否被占用
网络异常：支持重试与订单状态回查
5.5 计时页（用户端，进行中）
目标：清晰展示使用进度与可操作项（延长/提前结束）。
核心模块：
计时器：开始时间、已用时、预计结束
费用/权益：当前计费方式、已扣/待扣
操作区：延长、提前结束、联系商家
主要操作：
延长：选择延长时长→支付/扣卡→更新结束时间
提前结束：确认→结算→释放座位
关键规则与状态：
延长不可用：展示原因（已有预约/商家封座/超过营业时间）
即将结束提醒：提前N分钟弹窗+订阅通知
5.6 商家端：图纸编辑器
目标：低门槛创建自习室布局并发布线上可预约座位图。
核心模块：
画布设置：尺寸/比例、网格开关、对齐吸附
组件库：座位/通道/隔断/标识
属性面板：编号、标签、可预约、价格绑定
顶部操作：保存草稿、预览、发布、版本历史
主要操作：
拖拽放置座位→批量编号→设置标签与价格→保存草稿/发布
关键规则与状态：
发布校验：座位编号唯一、可预约座位至少1个、价格策略完整
冲突保护：存在未来预约的座位不允许删除（需引导改为“停用/封座”并处理预约）
5.7 商家端：座位状态看板
目标：实时掌握门店座位利用与异常，降低人工沟通成本。
核心模块：
状态筛选：空闲/待到场/使用中/即将结束/异常
座位卡片：座位号、用户、剩余时间、操作按钮
主要操作：
封座/解封、强制结束、换座、手动开始/结束
关键规则与状态：
所有强制操作需填写原因并留痕（用于纠纷处理）
六、UI 配色与视觉规范
6.1 色彩（建议）
品牌主色（Primary）：#2F80ED（清爽、可信）
强调色（Accent）：#27AE60（成功/可用座位）
警示色（Warning）：#F2994A（即将结束/提示）
危险色（Danger）：#EB5757（占用冲突/强制结束）
背景色（Background）：#F7F9FC
卡片底色（Surface）：#FFFFFF
文字主色（Text Primary）：#111827
次要文字（Text Secondary）：#6B7280
分割线（Border）：#E5E7EB
6.2 字体与层级
标题：18–20px，SemiBold
正文：14–16px，Regular
辅助信息：12–13px，Regular
数字计时（计时页）：24–32px，Medium/SemiBold（等宽数字优先）
6.3 组件规范（关键）
座位状态色/样式：
空闲可选：绿色描边/浅绿底
已预约：蓝色/灰蓝底（不可选）
使用中：深蓝或主色实心
封座/停用：灰色斜纹/锁图标
画布交互：双指缩放、单指拖拽；选中态高亮描边+阴影
七、交互与状态设计
7.1 预约流程状态机（用户）
可预约 → 已下单待开始（未到场） → 使用中（计时） → 已结束（完成）
取消分支：已下单待开始 → 已取消（按规则退费）
异常分支：
到时未开始：自动取消/自动开始/转异常（可配置）
超时未结束：自动结束结算/进入异常待处理（可配置）
7.2 座位状态（系统）
空闲：可被预约/可被延长占用
已预约：未来时段锁定
待到场：距离开始时间X分钟内（可提示/允许扫码开始）
使用中：计时中
封座：商家维护/不可预约
异常：冲突、强制结束待确认、支付失败等
7.3 关键交互细节
选座冲突处理：提交订单前与支付后各做一次“座位时段占用校验”，失败则提示并引导重新选择。
延长校验：展示“可延长上限”（到下一笔预约开始/到营业结束）与费用明细。
提前结束结算：明确计费规则（最低消费、进位规则、退款规则）并在确认弹窗展示。
通知策略：开始成功、即将结束、结束结算、卡券到期（订阅消息）。
7.4 权限与身份
用户端：微信授权登录（openid/unionid），手机号绑定用于通知与售后（可选但建议）。
商家端：账号登录/门店角色（老板/店员），操作留痕与权限分级（封座/强制结束为高权限）。
八、可扩展功能建议
多门店连锁：总部统一商品与规则，门店差异化价格与布局。
分区/房间：同一门店多个房间图纸与独立预约规则。
拼桌/团购：多人座位联动下单，支持团体预约。
会员体系：积分、等级、生日券、学习时长成就。
智能硬件联动：门禁/闸机、座位灯、插座通断电、蓝牙到店校验。
风控与黑名单：恶意占座、频繁取消限制、押金策略。
数据看板：座位利用率热力图、峰谷时段、商品售卖漏斗。
客服工单：纠纷处理、退款申诉、操作记录审计导出。